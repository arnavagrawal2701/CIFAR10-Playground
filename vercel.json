{
  "routes": [
    { "src": "/predict", "dest": "/api/app" },
    { "src": "/(.*)", "dest": "/api/app" }
  ],
  "rewrites": [
    { "source": "/api/app", "destination": "/api/app" }
  ],
  "builds": [
    { "src": "api/**", "use": "@vercel/python" }
  ]
}

